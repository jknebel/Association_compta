rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /receipts/{allPaths=**} {
      // Autorise la lecture/écriture seulement aux utilisateurs connectés.
      allow read, write: if request.auth != null;
    }
  }
}
